apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
patches:
  - path: patches/certificate.yaml
  - path: patches/cluster-secret-store.yaml
  - path: patches/db-service-account.yaml
  - path: patches/ingress.yaml
  - path: patches/secret-service-account.yaml