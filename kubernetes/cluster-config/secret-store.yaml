apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: gcp-secret-store
spec:
  provider:
    gcpsm:
      projectID: staging-57076
      auth:
        workloadIdentity:
          serviceAccountRef:
            name: external-secrets-operator-sa
            namespace: external-secrets-system
